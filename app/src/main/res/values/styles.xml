<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Widget.CalendarView.Custom" parent="android:Widget.CalendarView">
        <item name="android:focusedMonthDateColor">@color/colorWhite</item>
        <item name="android:weekNumberColor">@color/colorWhite</item>
        <item name="android:weekDayTextAppearance">@color/colorWhite</item>
        <item name="android:dateTextAppearance">@color/colorWhite</item>
    </style>

</resources>
